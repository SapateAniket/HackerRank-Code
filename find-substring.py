for _ in range(input()):
    st=map(str,raw_input().split())
    cnt=
    for _ in range(input()):
        chk=raw_input()
        le=len(chk)
        for i in range(len(st)):
            if st[i].find(chk) and st[i].index(chk)!=0 and st[i].index(chk)!=len(st[i])-le:
                cnt+=
        print cnt